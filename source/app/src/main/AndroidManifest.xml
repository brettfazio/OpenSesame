<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juleeyahwright.opensesame">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/gold_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gold_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_amber">
        <activity android:name=".ReportList.ReportListActivity" />
        <activity android:name=".AccountModel.AccountActivity"/>
        <activity
            android:name=".Settings.SettingsActivity"
            android:exported="true" />
        <activity android:name=".CreateReport.CreateReportActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".Map.MapActivity"
            android:exported="true" />
        <activity
            android:name=".ReportDetail.ReportDetailActivity"
            android:exported="true" />

        <activity android:name=".ReportAddInfo.ReportAddInfoActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>
</manifest>